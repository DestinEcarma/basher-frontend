import { useEffect } from "react";
import User from "../features/User.tsx"

const Topic = () => {
	useEffect(() => {

	}, []);

	return (
		<div>
			<User id={0}/>
		</div>
	)
};

export default Topic;
