import React from "react";
import { Children } from "@utils/defs";

export const LeftSide: React.FC<Children> = ({ children }) => {
	return children;
};

export const RightSide: React.FC<Children> = ({ children }) => {
	return children;
};
